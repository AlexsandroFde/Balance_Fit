import 'package:balance_fit/widgets/custom_title.dart';
import 'package:flutter/material.dart';

class ResultTitle extends StatelessWidget {
  const ResultTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const CustomTitle(title: 'Calorias Diárias Recomendadas');
  }
}